import 'package:flutter/material.dart';

class SingerTitle extends StatelessWidget {
  const SingerTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Furkan Can");
  }
}
